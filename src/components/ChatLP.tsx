interface Props {
  props: {
    topic: number
    description: React.ReactNode
    orientation: "left" | "right"
  }
}

export function ChatLP({ props }: Props) {
  return (
    <div className="">
      
    </div>
  )
}